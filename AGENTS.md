# 项目开发约束

本文件适用于整个仓库。任何实现或重构都必须遵守 `docs/core-principles.md`。

## 权威边界

- Python 后端是游戏状态和规则的唯一权威。
- 玩家输入、AI 输出和叙述文本都不能直接修改状态。
- 所有重要变化先形成经过校验的事件，再更新投影。
- 前端不得复制最终判定、物品所有权、关系、通缉或骰子逻辑。

## 实现要求

- 领域规则尽量写成不依赖网络、数据库和具体模型 SDK 的纯 Python 代码。
- AI 输出必须经过结构校验和领域校验。
- 修改事件结构时必须增加或更新 `schemaVersion` 处理策略。
- 每种改变状态的命令至少覆盖成功、前置条件失败和重复提交测试。
- 任何长期状态都必须保留来源事件或等价的可审计因果关系。
- 不得把语义向量检索结果直接当作权威事实。
- 人物能力和语言风格是带来源的资料/运行时上下文，不是自动判定结果；未知内容必须保留为空或 `unknown`，不能用 AI 推测直接写成 Canon 事实。
- 人物能力条目统一使用 `abilityId`、`name`、`level`、`sourceStatus`、`confidence`、`basis`、`sourceRefs`、`notes`；语言风格字段必须允许 `null`/空列表，不能为了完整而杜撰口头禅、方言或禁忌。

## 变更文档

如果实现需要违反现有架构决策，不要静默绕过；先新增 ADR 或更新对应规范，并说明迁移影响。

## 开发文档治理

- `docs/development.md` 是唯一持续更新的开发文档，记录当前模块焦点、边界、完成状态、测试范围和后续决定。
- `docs/current-development-status.md` 是保留的历史基线归档：不得删除、不得追加、不得回写当前开发进度，也不得把它作为当前任务依据。
- 其他路线图、阶段报告和历史规范只用于追溯背景；当前开发范围与模块切换一律以 `docs/development.md` 为准。

## 清理与验证成本

- 每次开发完成后检查并删除已经过期、无调用方或被正式实现替代的代码和文件；删除前先确认仓库内没有导入、脚本或文档引用，不能为了方便长期保留无用兼容入口。
- 历史事件和可回放数据属于事实记录，不因代码清理而改写；需要保留的历史兼容逻辑必须有明确用途和注释。
- 默认只运行与本次改动直接相关的小范围冒烟测试，并报告测试范围；只有改动核心事件、存储、回放、公共协议或用户明确要求时才运行全量回归。
