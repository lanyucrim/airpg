# 阶段 3A 验证报告

## 范围

阶段 3A 建立最小剧本包边界，并把“暮港纪事”初始世界从 Python 硬编码迁移到 `content/campaigns/gray-harbor/`。

## 已验证行为

- 合法剧本包可以加载；
- 不支持的 Schema 版本会被拒绝；
- 重复实体 ID 会被拒绝；
- 跨文件引用缺失会被拒绝；
- 同一内容生成稳定内容指纹和稳定初始事件；
- 内容发生变化时指纹发生变化；
- 剧本包只能编译为事件，再由事件重放得到状态；
- 初始场景、物品、关系与阶段 2 演示结果一致；
- 存档记录剧本标识、版本和内容指纹；
- 旧 SQLite 数据库可以原位补齐新字段且不丢失旧战役；
- 原有赠礼、关系来源、钥匙拒绝、虚假主张、复合行动、幂等和回合来源链保持有效。

## 自动验证

- Python 后端：29 项测试通过；
- 后端覆盖率：95%；
- 前端静态检查、生产构建和服务端渲染：通过；
- 文档结构检查：通过。

## 已知边界

- 当前自然语言解析和“守卫与酒”判定仍包含演示专用 ID；
- 当前只支持剧本包 Schema v1；
- 尚未提供第二个剧本包、网页编辑器或 AI 剧本生成；
- 本阶段没有改变网页视觉和玩家操作流程。

下一步是阶段 3B：把物品请求和给予抽象为任意角色、物品与容器之间的通用交互。
