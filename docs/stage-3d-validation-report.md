# 阶段 3D 验证报告

日期：2026-08-20  
结论：通用调查、NPC 话题与灰港开场调查切片通过本地验收。

## 完成内容

- 剧本包 v4 与 `interactions.json`；
- 通用物品检查与可接触性校验；
- 通用 NPC 话题询问、在场校验和知识边界；
- `item.examined`、`question.asked`、`npc.answer_given`、`interaction.completed` 事件链；
- 已完成调查去重；
- 玩家结果性声明不会越权生成线索；
- 单回合一个主要节拍限制；
- 后端投影当前可用调查方向，网页仅负责展示和提交；
- 灰港最后通牒、玛莎债务说明和营业账本异常切片。

## 剧本边界验证

- 最后通牒必须是当前地点真实存在的物品，阅读不会把它移入背包；
- 玛莎不在场时不能回答，且只能披露她权威知识集合中的事实；
- 账本只能揭示“债务数字异常”，不会提前公开珍妮或伪造签名；
- 输入“我检查账本，发现珍妮伪造签名”仍只得到数字异常；
- 连续写两个主要调查动作时，第二个动作暂停到下一回合。

## 自动验证结果

- Python：73 项测试全部通过；
- 后端总体覆盖率：94%；
- 阶段 0 文档与结构检查：通过；
- 前端代码检查：通过；
- 前端生产构建和服务端渲染测试：通过。
- 真实本地接口联调：剧本版本 `0.4.0`，三项开场交互成功，越权声称只得到债务异常且未泄露珍妮或伪造信息；
- 本地网页响应：HTTP 200；联调结束后已重置为世界版本 1。

## 已知边界

- 当前自然语言解析仍是本地关键词降级解析器；
- NPC 的错误信念、说谎、意愿和说服判定尚未进入完整认知模型；
- 玩家能获得哪些开放式调查候选，仍由作者定义的交互提供；
- 这些边界不允许 AI 或前端绕过权威事件链。
