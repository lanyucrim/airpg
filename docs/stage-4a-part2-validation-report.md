# 阶段 4A 第二部分验证报告

日期：2026-08-20  
结论：DeepSeek 真实意图解析纵向切片通过本地验收；小型评估通过，但仍需扩充为长期回归集。

## 已完成

- DeepSeek Chat Completions JSON 适配器；
- 后端环境开关与默认关闭策略；
- 非思考/思考模式切换；
- 固定超时、输出预算和单次瞬时错误重试；
- 模型提供方、模型、输入/输出令牌和延迟审计；
- 普通接口原始请求与输出脱敏；
- 模型调用移出 SQLite 写事务，并在提交前二次校验版本与幂等键；
- 无上下文“那边”等目标猜测的领域拒绝；
- 网页开发追踪显示真实模型、令牌和延迟。

## 真实模型冒烟评估

最终配置：`deepseek-v4-flash`、非思考模式、无重试。

- 用例：8；
- 通过：8；
- 最终通过率：100%；
- 总令牌：6,512；
- 平均延迟：1,076 ms；
- 禁止隐藏 ID 泄漏：0；
- 覆盖：通牒自然语言检查、玛莎债务询问、移动、等待、结果性声明、复合节拍、模糊目的地、隐藏出口诱导。

首轮非思考模式为 6/8，暴露了模糊目标可能被猜测的问题。增加提示约束和后端领域拒绝后，同一批评估达到 8/8。思考模式对照更慢，并出现一次无效交互引用，因此当前不作为默认模式。

## 真实网页回合

- 状态接口：DeepSeek 已启用；
- 输入：“我想凑近桌上那份帮派文书，逐行看看它写了什么。”；
- 解析来源：`model`；
- 权威结果：`inspection_completed`；
- 令牌：837；
- 延迟：1,094 ms；
- 玩家接口中的原始模型请求与输出：均已脱敏；
- 网页：HTTP 200。

## 自动验证

- Python：90 项测试全部通过；
- 后端总体覆盖率：93%；
- 覆盖模拟 HTTP、JSON 输出、思考开关、重试上限、超时、环境关闭、领域校验、事务锁、旧数据库迁移、审计和幂等；
- 前端代码检查、生产构建与服务端渲染测试：通过；
- 阶段 0 文档与结构检查：通过（14 个必需文件、37 个 Markdown 文件）；
- 收尾后战役恢复为状态版本 1 的白鹭屋正式开局，DeepSeek 仍在本地后端启用。

## 尚未完成

- 8 项是冒烟集，不足以代表长期自由输入；
- 尚未建立版本化大样本、P95 延迟、费用趋势和发布阈值；
- 尚未实现按动作复杂度自动选择思考模式；
- AI 叙述器仍未接入。
