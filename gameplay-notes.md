# 游玩反馈与待改进方向

## 1. 世界发展与剧情推进

- 当前整个世界过于依赖主角推进。
- 世界应该拥有自身的发展轨迹，剧情既可以由主角主动推动，也可以由世界状态和其他角色被动推动。
- 这一方向既需要修改剧本设计，也需要修改 AI 逻辑。

## 2. 新增内容的状态归属

- 需要完善存储与归属系统，避免游戏中莫名其妙出现没有来源或位置的内容。
- 例如：新增一份合同后，需要主动将合同放入主角的背包。
- 例如：新增一栋房子后，需要修改代码，将这栋房子放入对应的地点位置。

## 3. AI 新增内容必须进入核心系统

- AI 构建出的新增任务，需要同步补充进代码核心系统，不能只存在于 AI 叙述或临时文本中。
- AI 新增的人物，也需要同步补充进代码核心系统，不能只存在于叙述文本中。

## 4. 每日精力限制

- 应限制主角每天可使用的精力，避免主角在一天内处理过多事情。

## 5. 允许无结果的行动

- 主角的行动不应保证必然产生结果。
- 例如：主角花一整个下午跟踪某人，最后可能什么结果都没有。
- 玩家可以把整天用于挣钱、做工、营业、休息、睡觉、散步、发呆或等待。只要统一世界时间正常推进，这一天不需要产出线索、奖励或剧情节拍。
- “选择挣钱”与“获得收入”必须分开：在经济规则尚未为该岗位生成可确认事件前，系统只能记录玩家花了时间，不能凭空增加金钱；未来接入收入规则后，允许结果为收入、低收入或没有收入。
- 世界照常运行，但不能因为玩家休息或发呆就专门制造灾难惩罚她；当天只结算自然发生的日级变化，周/月级事件按自己的时间尺度推进。

## 5.1 零推进日与叙事长度

- 跑团没有固定“建议篇幅”、章节团次数或每回合字数目标。章节只定义世界条件、硬期限和可能的权力门槛，不规定玩家必须用多少次团完成。
- 任何一天都可能只是生活：挣钱、处理家务、陪伴关系、恢复伤病、等待消息、失败、发呆，或者什么都没有改变。AI 不得为了让回合显得有内容而强行添加线索、奖励、NPC 反应或主线进展。
- 长篇幅来自世界实际发生的历史和玩家选择，而不是人为拉长或压缩。叙述应在确认结果后尽快归还控制权。

## 6. 任务解决方式与剧本约束

- 很多任务的解决方法已经写进剧本。
- 在处理这些任务时，应尽可能不要让玩家通过剧本之外的方法解决。
- 如果玩家提出的替代方法特别合理，可以允许采用其他解决方式。

## 7. 网页交互与地图信息架构（本轮已落地的边界）

- 网页现在使用后端公开状态中的地图投影，展示城市、城区、街道、建筑、楼层和房间的层级名称。
- 地点可以标记为 `public`、`player` 或 `gm` 地图可见性；`gm` 地点不会进入玩家状态，`player` 地点只有在当前路径或发现后才显示。
- 地图上的可达地点可以直接点击。点击只提交一个普通的“前往某地”行动，移动距离、出口可见性、锁定、秘密通道和时间消耗仍由 Python 后端判定。
- 地图不返回地点私密描述，避免把地窖秘密、GM 位置或尚未编译的剧本事实通过 UI 泄漏。
- NPC 的完整位置仍属于权威内部状态；玩家地图只显示与主角当前同处的 NPC，不能借地图扫描整座城市的人物行踪。
- 新增建筑、房屋、人物和任务必须继续进入剧本包、事件和位置归属链，不能只在地图组件中添加一行文字。

## 8. 后续网页状态面板（暂缓到对应规则阶段）

- 精力、饱腹度、睡眠、伤病恢复和能力条：等阶段 8 的完整跑团规则与休息规则确定后再接入，当前网页的体力/专注只是已有纵向切片状态，不代表 V4.2 的最终属性模型。
- 七城区完整地图、建筑清单和 NPC 日程：等阶段 6/7 的认知传播、世界周报和自治时间线建立后分批导入，不能用静态坐标假装世界会自行运行。
- 任务状态机、剧本限定解法和合理替代解法：等阶段 7/8 的任务与导演规则完成后接入，所有任务必须可追溯到来源事件。
- 后期授权、事务桌、财务、人事和组织管理：等阶段 9 的开发者工具和组织系统完成后实现。

## 9. 阶段 5—7 实现后的游玩约定

- 长期记忆只来自确认事件。玩家声称发生过的事、叙述中的补充和 AI 临时提出的任务都不会自动进入历史。
- 角色知道什么取决于来源、渠道、时间和可信度。通缉、犯罪和秘密会延迟传播；不同 NPC 可以拥有相反的相信、怀疑或否认。
- 城市会按日、周、月自行结算。等待、工作、休息、发呆和失败都可以是完整回合，不要求每天推动主线，也没有固定建议篇幅、字数或团次数。
- “等待一周”“等待一个月”是明确的长时间行动，会跨越并逐个结算周报/月报边界；不会被降级成默认十分钟等待。
- 网页玩家模式默认显示地图、地点层级、当前同处人物、可见出口、背包、公开期限、线索、未解决问题和城市周报。开发来源链只通过 `?mode=developer` 受控打开。
- 当前唯一权威剧本是 V4.2：`灰港_黑潮王座_V4.2_AI_GM主线状态机与支线条件版.md`。阶段 7.5A 已把它编译为带 SHA-256、来源行号、片段指纹和 C0/C1/C2/G Canon 层级的结构化目录。新增地点、人物、物品、任务或组织必须先进入剧本包/目录，再由事件写入世界，不能只写在叙述里；旧 V3 目录仅用于历史存档迁移，不参与新战役加载。
- 普通环境搜索和秘密调查是两类动作。搜索厨房可以找到真实食物；找到不等于拿走，取用必须另起权威事件。没有结果也会正常消耗时间并返回“没有找到合适的东西”。
- 擦桌、整理餐具、打扫、收拾等合理日常行为会记录为带地点来源的环境行动并推进少量时间；它们不会自动制造物品、关系、收入或剧情结果，也不会被误判成对 NPC 发言。
- 后续日常自治不采用“地点动作白名单”。地点提供的是来源、时间、社会环境、资源类别和影响上限；AI 可以在边界内生成街边摊、普通路人、临时工作、吃饭和闲聊等合理机会，程序只确认来源、资源、权限和故事影响。
- AI 日常候选默认只能产生 `mundane` 普通物品与 `routine/soft` 影响。普通食物、饮品、纸张、绳子或小礼物可以在剧本允许时成为通用材料或社交资源，但不能自行变成主线关键道具。
- 和 NPC 吃饭、散步、陪伴或闲聊可以改变熟悉度、人情或短期印象，但不能自动完成任务、披露秘密或推进主要剧情；好感度也不能单独决定借钱和重大帮助，NPC 的知识、资源、人格、职责、时间和底线必须共同参与。
- 这套机制属于阶段 7.5。由于现有 `engine.py`、`scenario.py`、`projection.py`、`npc_decision.py` 和网页主组件是阶段纵向切片，必须按 7.5A—7.5I 逐步拆分：先做 V4.2 编译，再做权威内核迁移门，随后迁移环境、物品、AI、NPC、世界和网页。禁止一次性重写；旧事件和存档必须继续可重放，并在每次切流前比较新旧投影摘要。
- 受限物品被拿走时会记录真实物品转移和法律风险，但不会自动生成目击、报告、证据或全城通缉；这些后果必须按来源链逐步发生。

## 10. 阶段 8 预留

- 精力、饱腹度、睡眠、能力条、技能、骰子、战斗和收入规则尚未作为阶段 5—7 的完成条件；网页目前保留可扩展状态位，不把占位值当成最终规则。

## 11. AI 驱动游戏的具体优化方案（新增冻结要求）

这一节把“AI 作为底层驱动、剧本作为限制条件、程序负责防止跑偏”落实为可以开发和验收的规则。它不是让 AI 随意写世界，而是让 AI 在真实的地点、时间、资源和人物边界内提供玩家没有预先写出的日常可能性。

### 11.1 日常行为不再由地点白名单决定

- 玩家输入先由 AI 识别为搜索、观察、取用、购买、用餐、工作、社交、休息、移动、调查或其他合理日常意图。
- 程序根据当前位置公开机会、时间、营业状态、天气、库存、价格、在场人物和玩家资源判断能否尝试。
- 结果必须区分：成功、部分成功、没有找到、售罄、买不起、关门、被拒绝、延期和需要下一步确认。
- “没有找到结果”仍然是完整回合：写入搜索/行动事件，消耗合理时间，返回自然叙述，不改写成隐藏通路失败。
- AI 不能把合理日常行为自动升级成秘密发现、主线推进或重大后果；隐藏通路和剧本调查仍使用独立条件。

### 11.2 统一物品标准

每个物品先有定义，再有实例。无论来自 V4.2 剧本、商店库存还是日常 AI 候选，都必须至少具备：

`itemDefinitionId`、名称与别名、类别、标签、是否可堆叠、重量/体积、基础价值、状态模型、标准操作、法律状态、关键性、故事绑定政策、实例 ID、数量、容器、所有者、创建来源事件、最近变更事件和可选过期时间。

标准操作固定为 `inspect`、`take`、`transfer`、`consume`、`use`、`equip`、`combine`、`discard`、`destroy`、`expire`。前端只能发起操作请求，后端验证所有权、位置、数量、权限、状态和来源后提交原子事件。

日常 AI 生成物品默认是 `mundane`，最多成为普通食物、饮料、材料、礼物或小额交易对象。剧本声明允许时，它可以满足非唯一的通用条件；要进入主线/支线必须经过带来源的 `story.item_bound` 事件，不能由叙述直接晋升为关键道具。

### 11.3 NPC 好感不是行为开关

- 关系至少分为熟悉度、即时心情、人情、信任、尊重、恐惧、怀疑和债务；这些是可追溯历史的摘要，不是行为阈值。
- NPC 每次决定还要读取自己的知识/传闻、可支配资源、职责、人格、日程、风险、法律底线和请求的具体代价。
- AI 只提出 `accept`、`decline`、`partial`、`conditional`、`defer`、`redirect` 或 `conceal` 候选；程序检查 NPC 是否知道、是否有资源、是否在场、是否有权限以及是否触碰硬边界。
- 同一关系摘要在不同资源和时间下可以产生不同结果；高好感但没钱可以拒绝借钱，低好感但有抵押也可以有限合作。
- 一起吃饭、散步、陪伴和闲聊只产生有限熟悉度、人情或短期印象，不能自动披露秘密、完成任务或推进主线。

### 11.4 大幅代码重构的实施顺序

这部分明确承认旧代码需要大幅修改，但采用可回滚的逐领域迁移：

1. 先把 `engine.py` 变成薄回合协调器，再把投影分成注册式事件处理器；旧事件通过版本适配器重放。
2. 将环境机会、日常行动、无结果和临时机会迁到独立领域模块，移除旧关键词分支。
3. 将物品定义/实例、容器、所有权、交易和消费迁到统一库存领域；任何物品状态变化都走原子事件。
4. 将 NPC 上下文组装、模型调用和硬规则验证拆开；关系变化必须带事件来源。
5. 将日/周/月自治、库存、价格和 NPC 日程从 Director 拆成世界模拟模块；等待和缺席也要按时间结算。
6. 将网页主组件拆为地图、场景、背包、行动输入、结果反馈和开发追踪；玩家模式默认不显示内部候选和私密理由。

每一步都要通过旧存档回放、新旧投影摘要比较、离线契约测试和网页玩家验收；没有通过迁移门时，旧入口继续工作，不删除历史事件。

### 11.5 后续开发阶段

- **7.5B：权威内核迁移门**：先做端口、事件版本、投影对比和幂等保护。
- **7.5C：环境机会与日常行动**：覆盖街道找食物、临时工作、吃饭、休息、发呆和无结果回合。
- **7.5D：统一物品与背包生命周期**：覆盖定义/实例、取用、购买、转移、消费、使用、过期和故事绑定。
- **7.5E：受约束的 AI 日常导演**：AI 生成候选，程序验证来源、资源、影响等级和降级。
- **7.5F：NPC 主观决策与社会关系**：用知识、资源、人格、日程、风险和关系历史共同决定反应。
- **7.5G：世界自治与经济循环**：接入库存、价格、工作、组织计划和玩家缺席时的日/周/月变化。
- **7.5H：玩家网页与地图背包交互**：让网页成为可点击、可输入、可查看状态的实际游戏界面。
- **7.5I：自动评估、迁移和真实玩家验收**：先记录失败样本，再修复规则，最后重复全流程验收。

阶段 8 才加入精力、饱腹度、睡眠、技能、骰子、战斗和完整收入效果；7.5 只为这些字段预留接口，不提前伪造规则。
